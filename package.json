{
  "name": "gridcoin-stamp-action",
  "version": "0.0.0",
  "private": true,
  "description": "GitHub Action to timestamp release assets on the Gridcoin blockchain via stamp.gridcoin.club",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && ncc build src/index.ts -o dist --license licenses.txt",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "all": "npm run typecheck && npm test && npm run build",
    "prepare": "husky || true"
  },
  "keywords": [
    "github-action",
    "gridcoin",
    "blockchain",
    "timestamp",
    "notarize"
  ],
  "author": "gridcat",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^2.0.3",
    "@actions/github": "^8.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "@vercel/ncc": "^0.38.3",
    "husky": "^9.1.7",
    "typescript": "^5.7.0",
    "vitest": "^4.1.4"
  }
}
